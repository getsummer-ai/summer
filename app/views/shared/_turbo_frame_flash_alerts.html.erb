<% is_persistent ||= false %>
<% turbo_frame_tag_id ||= '' %>

<% if turbo_frame_request? && turbo_frame_tag_id.empty? || turbo_frame_request_id == turbo_frame_tag_id  %>
  <%= render 'shared/flash_alerts',  is_persistent: is_persistent %>
<% end %>
