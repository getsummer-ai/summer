<h2 class="font-semibold text-2xl">Actions</h2>
<p class="opacity-50 mt-1">
  Summer will appear on all the pages from your domain link with more that 500 symbols. Each page is
  being added to the list after first Summer appearance. Turn Summer on and off manually per page if
  needed.
</p>

<div id="summer_feature_cards" class="grid gap-6 lg:grid-cols-2 lg:grid-rows-2 grid-cols-1 grid-rows-4 mt-12">
  <%= render 'cards_list' %>
</div>
