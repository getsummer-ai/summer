<%= turbo_stream.prepend "flash", partial: "layouts/shared/alerts" %>

<%= turbo_stream.replace dom_id(@article_product, :switch) do %>
  <%=render ProjectPageProductComponents::SwitchButtonComponent.new(
      form_url: project_page_product_path(
        current_project,
        @project_page,
        @article_product
      ),
      project_article_product: @article_product,
    ) %>
<% end %>
