<div class="shadow p-5 rounded-xl border">
  <div class="flex justify-between">
    <span class="align-middle"><%=title %></span>
    <img src="<%= icon_src %>" class="summer-icon-sm opacity-50" alt="Icon" />
  </div>
  <div class="text-2xl mt-1 font-semibold"><%=number %></div>
  <div class="text-sm mt-1 opacity-50"><%=comment %></div>
</div>
