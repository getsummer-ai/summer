<%= turbo_stream.replace @project_page do %>
  <%= render ProjectPageRowComponent.new(project: @current_project, page: @project_page) %>
<% end %>
<%= turbo_stream.prepend "flash", partial: "layouts/shared/alerts" %>
